[project]
name = "career-twin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ignacioaj", email = "ignacioatencia@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
career-twin = "career_twin:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
